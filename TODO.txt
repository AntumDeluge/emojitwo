[priority]
scripts:
- handle excessive or stray commas in comma-separated key=value arguments
- allow list/tuple as include/exclude argument for getReleaseImages & getImagesToRemove functions
- organize images in theme file

[non-priority]
scripts:
- format spacing in usage output
- add "conflicts" options for command line arguments
- only include explicitely declared sizes in release zip archive
- update usage string dynamically when ArgsObject.registerKey called
- handle heart character with unknown trailing character data (❤️) sent by iPhones (?)
- add configuration for text replacement plugin
- allow combining short arguments (e.g.: '-nOs', following values should be for last argument)
